import{_ as t,M as o,p as d,q as c,R as a,t as e,N as i,a1 as n}from"./framework-e8cb8151.js";const l={},p=n(`<h1 id="_06-浏览器如何渲染页面" tabindex="-1"><a class="header-anchor" href="#_06-浏览器如何渲染页面" aria-hidden="true">#</a> 06-浏览器如何渲染页面？</h1><p>这一课时我将结合代码实例为你讲解浏览器渲染页面时的流程和步骤。</p><p>先来看一个例子，假如我们在浏览器中输入了一个网址，得到了下面的 html 文件，渲染引擎是怎样通过解析代码生成页面的呢？</p><div class="language-html line-numbers-mode" data-ext="html"><pre class="language-html"><code><span class="token doctype"><span class="token punctuation">&lt;!</span><span class="token doctype-tag">DOCTYPE</span> <span class="token name">html</span><span class="token punctuation">&gt;</span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>html</span><span class="token punctuation">&gt;</span></span>
  <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>head</span><span class="token punctuation">&gt;</span></span>
  <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>head</span><span class="token punctuation">&gt;</span></span>
  <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>body</span><span class="token punctuation">&gt;</span></span>
    la gou
  <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>body</span><span class="token punctuation">&gt;</span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>html</span><span class="token punctuation">&gt;</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="从-html-到-dom" tabindex="-1"><a class="header-anchor" href="#从-html-到-dom" aria-hidden="true">#</a> 从 HTML 到 DOM</h2><h3 id="_1-字节流解码" tabindex="-1"><a class="header-anchor" href="#_1-字节流解码" aria-hidden="true">#</a> 1. 字节流解码</h3>`,6),r=a("code",null,"HTTP 协议",-1),h={href:"https://html.spec.whatwg.org/multipage/parsing.html#encoding-sniffing-algorithm",target:"_blank",rel:"noopener noreferrer"},u=n(`<p>这个把字节数据解码成字符数据的过程称之为“<strong>字节流解码</strong>”。</p><p><img src="https://s0.lgstatic.com/i/image/M00/12/EE/CgqCHl7OM8-AJ2jzAABvLLJLW2s663.png" alt="image (4).png"></p><p>我们通过浏览器调试工具查看网络请求时，也是经过了上述操作过程，才能直观地看到字符串。</p><h3 id="_2-输入流预处理" tabindex="-1"><a class="header-anchor" href="#_2-输入流预处理" aria-hidden="true">#</a> 2. 输入流预处理</h3><p>通过上一步解码得到的字符流数据在进入解析环节之前还需要进行一些预处理操作。比如将换行符转换成统一的格式，最终生成规范化的字符流数据，这个把字符数据进行统一格式化的过程称之为“<strong>输入流预处理</strong>”。</p><h3 id="_3-令牌化" tabindex="-1"><a class="header-anchor" href="#_3-令牌化" aria-hidden="true">#</a> 3. 令牌化</h3><p>经过前两步的数据解码和预处理，下面就要进入重要的解析步骤了。</p><p>解析包含两步，第一步是<strong>将字符数据转化成令牌（Token）</strong>，第二步是<strong>解析 HTML 生成 DOM 树</strong>。先来说说令牌化，其过程是使用了一种类似状态机的算法，即每次接收一个或多个输入流中的字符；然后根据当前状态和这些字符来更新下一个状态，也就是说在不同的状态下接收同样的字符数据可能会产生不同的结果，比如当接收到“body”字符串时，在标签打开状态会解析成标签，在标签关闭状态则会解析成文本节点。</p><p>这个算法的解析规则较多，在此就不一一列举了，有兴趣的同学可以通过下面这个简单的例子来理解其原理。</p><p>上述 <code>html</code> 代码的标记过程如下：</p><ol><li>初始化为“数据状态”（Data State）；</li><li>匹配到字符 <code>&lt;</code>，状态切换到 “<code>标签打开状态</code>”（Tag Open State）；</li><li>匹配到字符 <code>!</code>，状态切换至 “<code>标签声明打开状态</code>”（Markup Declaration Open State），后续 7 个字符可以组成字符串 <code>DOCTYPE</code>，跳转到 “<code>DOCTYPE 状态</code>”（DOCTYPE State）；</li><li>匹配到字符为<code>空格</code>，当前状态切换至 “<code>DOCTYPE 名称之前状态</code>”（Before DOCTYPE Name State）；</li><li>匹配到字符串 <code>html</code>，创建一个新的 DOCTYPE 标记，标记的名字为 “html” ，然后当前状态切换至 “<code>DOCTYPE 名字状态</code>”（DOCTYPE Name State）；</li><li>匹配到字符 <code>&gt;</code>，跳转到 “<code>数据状态</code>” 并且释放当前的 DOCTYPE 标记；</li><li>匹配到字符 <code>&lt;</code>，切换到 “<code>标签打开状态</code>”；</li><li>匹配到字符 <code>h</code>，创建一个新的起始标签标记，设置标记的标签名为空，当前状态切换至 “<code>标签名称状态</code>”（Tag Name State）；</li><li>从字符 <code>h</code> 开始解析，将解析的字符一个一个添加到创建的起始标签标记的标签名中，直到匹配到字符 <code>&gt;</code>，此时当前状态切换至 “数据状态” 并释放当前标记，当前标记的标签名为 “<code>html</code>” 。</li><li>解析后续的 的方式与 一致，创建并释放对应的起始标签标记，解析完毕后，当前状态处于 “<code>数据状态</code>” ；</li><li>匹配到字符串 “标记” ，针对每一个字符，创建并释放一个对应的字符标记，解析完毕后，当前状态仍然处于 “数据状态” ；</li><li>匹配到字符 <code>&lt;</code>，进入 “<code>标签打开状态</code>” ；</li><li>匹配到字符 <code>/</code>，进入 “<code>结束标签打开状态</code>”（End Tag Open State）；</li><li>匹配到字符 <code>b</code>，创建一个新的结束标签标记，设置标记的标签名为空，当前状态切换至“<code>标签名称状态</code>”（Tag Name State）；</li><li>重新从字符 <code>b</code> 开始解析，将解析的字符一个一个添加到创建的结束标签标记的标签名中，直到匹配到字符 <code>&gt;</code>，此时当前状态切换至 “<code>数据状态</code>” 并释放当前标记，当前标记的标签名为 “<code>body</code>”；</li><li>解析 的方式与 一样；</li><li>所有的 html 标签和文本解析完成后，状态切换至 “<code>数据状态</code>” ，一旦匹配到文件结束标志符（EOF），则释放 <code>EOF 标记</code>。</li></ol><p>最终生成类似下面的令牌结构：</p><div class="language-html line-numbers-mode" data-ext="html"><pre class="language-html"><code>开始标签: html
  开始标签: head
  结束标签: head
  开始标签: body
    字符串: la gou
  结束标签: body
结束标签: html
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="补充-1-遇到-script-标签时的处理" tabindex="-1"><a class="header-anchor" href="#补充-1-遇到-script-标签时的处理" aria-hidden="true">#</a> 补充 1：遇到 script 标签时的处理</h3><p>如果在 HTML 解析过程中遇到 script 标签，则会发生一些变化。</p><p>如果遇到的是内联代码，也就是在 script 标签中直接写代码，那么解析过程会暂停，执行权限会转给 JavaScript 脚本引擎，待 JavaScript 脚本执行完成之后再交由渲染引擎继续解析。有一种情况例外，那就是脚本内容中调用了改变 DOM 结构的 document.write() 函数，此时渲染引擎会回到第二步，将这些代码加入字符流，重新进行解析。</p><p>如果遇到的是外链脚本，那么渲染引擎会按照我们在第 01 课时中所述的，根据标签属性来执行对应的操作。</p><h3 id="_4-构建-dom-树" tabindex="-1"><a class="header-anchor" href="#_4-构建-dom-树" aria-hidden="true">#</a> 4. 构建 DOM 树</h3><p>解析 HTML 的第二步是树构建。</p><p>浏览器在创建解析器的同时会创建一个 Document 对象。在树构建阶段，Document 会作为根节点被不断地修改和扩充。标记步骤产生的令牌会被送到树构建器进行处理。HTML 5 标准中定义了每类令牌对应的 DOM 元素，当树构建器接收到某个令牌时就会创建该令牌对应的 DOM 元素并将该元素插入到 DOM 树中。</p><p>为了纠正元素标签嵌套错位的问题和处理未关闭的元素标签，树构建器创建的新 DOM 元素还会被插入到一个开放元素栈中。</p><p>树构建算法也可以采用状态机的方式来描述，具体我们以步骤 1 的 HTML 代码为例进行举例说明。</p><ol><li>进入初始状态 “initial” 模式；</li><li>树构建器接收到 DOCTYPE 令牌后，树构建器会创建一个 DocumentType 节点附加到 Document 节点上，DocumentType 节点的 name 属性为 DOCTYPE 令牌的名称，切换到 “before html” 模式；</li><li>接收到令牌 html 后，树构建器创建一个 html 元素并将该元素作为 Document 的子节点插入到 DOM 树中和开放元素栈中，切换为 “before head” 模式；</li><li>虽然没有接收到 head 令牌，但仍然会隐式地创建 head 元素并加到 DOM 树和开放元素栈中，切换到“in head”模式；</li><li>将开放元素栈中的 head 元素弹出，进入 “after head”模式；</li><li>接收到 body 令牌后，会创建一个 body 元素插入到 DOM 树中同时压入开放元素栈中，当前状态切换为 “in body” 模式；</li><li>接收到字符令牌，创建 Text 节点，节点值为字符内容“标记”，将 Text 节点作为 body 元素节点插入到 DOM 树中；</li><li>接收到结束令牌 body，将开放元素栈中的 body 元素弹出，切换至 “after body” 模式；</li><li>接收到结束令牌 html，将开放元素栈中的 html 元素弹出，切换至 “after after body” 模式；</li><li>接收到 EOF 令牌，树构建器停止构建，html 文档解析过程完成。</li></ol><p>最终生成下面的 DOM 树结构：</p><div class="language-plain line-numbers-mode" data-ext="plain"><pre class="language-plain"><code>              Document
             /        \\
DocumentType           HTMLHtmlElement
                      /               \\
       HTMLHeadElement                 HTMLBodyElement
                                              |
                                          TextNode
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><img src="https://s0.lgstatic.com/i/image/M00/12/EE/CgqCHl7OM-CAQGiGAAFv6uHi6MI573.png" alt="1.png"></p><h3 id="补充-2-从-css-到-cssom" tabindex="-1"><a class="header-anchor" href="#补充-2-从-css-到-cssom" aria-hidden="true">#</a> 补充 2：从 CSS 到 CSSOM</h3><p>渲染引擎除了解析 HTML 之外，也需要解析 CSS。</p><p>CSS 解析的过程与 HTML 解析过程步骤一致，最终也会生成树状结构。</p><p>与 DOM 树不同的是，CSSOM 树的节点具有继承特性，也就是会先继承父节点样式作为当前样式，然后再进行补充或覆盖。下面举例说明。</p><div class="language-css line-numbers-mode" data-ext="css"><pre class="language-css"><code><span class="token selector">body</span> <span class="token punctuation">{</span> <span class="token property">font-size</span><span class="token punctuation">:</span> 12px <span class="token punctuation">}</span>
<span class="token selector">p</span> <span class="token punctuation">{</span> <span class="token property">font-weight</span><span class="token punctuation">:</span> lighter <span class="token punctuation">}</span>
<span class="token selector">span</span> <span class="token punctuation">{</span> <span class="token property">color</span><span class="token punctuation">:</span> blue <span class="token punctuation">}</span>
<span class="token selector">p span</span> <span class="token punctuation">{</span> <span class="token property">display</span><span class="token punctuation">:</span> none <span class="token punctuation">}</span>
<span class="token selector">img</span> <span class="token punctuation">{</span> <span class="token property">float</span><span class="token punctuation">:</span> left <span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>对于上面的代码，会解析生成类似下面结构的 DOM 树：</p><p><img src="https://s0.lgstatic.com/i/image/M00/12/E2/Ciqc1F7OM-mAO8T2AAGu2Fxum4w061.png" alt="2.png"></p><p>需要注意的是，上图中的 <code>CSSOM</code> 树并不完整，完整的 <code>CSSOM</code> 树还应当包括浏览器提供的默认样式（也称为“User Agent 样式”）。</p><h2 id="从-dom-到渲染" tabindex="-1"><a class="header-anchor" href="#从-dom-到渲染" aria-hidden="true">#</a> 从 DOM 到渲染</h2><p>有了 <code>DOM</code> 树和 <code>CSSOM</code> 树之后，渲染引擎就可以开始生成页面了。</p><h3 id="_5-构建渲染树" tabindex="-1"><a class="header-anchor" href="#_5-构建渲染树" aria-hidden="true">#</a> 5. 构建渲染树</h3><p><code>DOM</code> 树包含的结构内容与 <code>CSSOM</code> 树包含的样式规则都是独立的，为了更方便渲染，先需要将它们合并成一棵渲染树。</p><p>这个过程会从 <code>DOM</code> 树的根节点开始遍历，然后在 <code>CSSOM</code> 树上找到每个节点对应的样式。</p><p>遍历过程中会自动忽略那些不需要渲染的节点（比如脚本标记、元标记等）以及不可见的节点（比如设置了“<code>display:none</code>”样式）。同时也会将一些需要显示的伪类元素加到渲染树中。</p><p>对于上面的 <code>HTML</code> 和 <code>CSS</code> 代码，最终生成的渲染树就只有一个 <code>body</code> 节点，样式为 <code>font-size:12px</code>。</p><h3 id="_6-布局" tabindex="-1"><a class="header-anchor" href="#_6-布局" aria-hidden="true">#</a> 6. 布局</h3><p>生成了渲染树之后，就可以进入布局阶段了，布局就是计算元素的大小及位置。</p><p>计算元素布局是一个比较复杂的操作，因为需要考虑的因素有很多，包括字体大小、换行位置等，这些因素会影响段落的大小和形状，进而影响下一个段落的位置。</p><p>布局完成后会输出对应的“盒模型”，它会精确地捕获每个元素的确切位置和大小，将所有相对值都转换为屏幕上的绝对像素。</p><h3 id="_7-绘制" tabindex="-1"><a class="header-anchor" href="#_7-绘制" aria-hidden="true">#</a> 7. 绘制</h3><p>绘制就是将渲染树中的每个节点转换成屏幕上的实际像素的过程。得到布局树这份“施工图”之后，渲染引擎并不能立即绘制，因为还不知道绘制顺序，如果没有弄清楚绘制顺序，那么很可能会导致页面被错误地渲染。</p><p>例如，对于使用 <code>z-index</code> 属性的元素（如遮罩层）如果未按照正确的顺序绘制，则将导致渲染结果和预期不符（失去遮罩作用）。</p><p>所以绘制过程中的第一步就是遍历布局树，生成绘制记录，然后渲染引擎会根据绘制记录去绘制相应的内容。</p><p>对于无动画效果的情况，只需要考虑空间维度，生成不同的图层，然后再把这些图层进行合成，最终成为我们看到的页面。当然这个绘制过程并不是静态不变的，会随着页面滚动不断合成新的图形。</p><h2 id="总结" tabindex="-1"><a class="header-anchor" href="#总结" aria-hidden="true">#</a> 总结</h2><p>这一课时主要讲解了浏览器渲染引擎生成页面的 7 个步骤，前面 4 个步骤为 DOM 树的生成过程，后面 3 个步骤是利用 DOM 树和 CSSOM 树来渲染页面的过程。我们想要理解和记忆这些过程其实很简单，那就是以数据变化为线索，具体来说数据的变化过程为：</p><p><strong>字节 → 字符 → 令牌 → 树 → 页面</strong></p><p>最后布置一道思考题：在构建渲染树的时候，渲染引擎需要遍历 DOM 树节点并从 CSSOM 树中找到匹配的样式规则，在匹配过程中是通过自上而下还是自下而上的方式呢？为什么？</p><hr><h3 id="精选评论" tabindex="-1"><a class="header-anchor" href="#精选评论" aria-hidden="true">#</a> 精选评论</h3><h5 id="锐" tabindex="-1"><a class="header-anchor" href="#锐" aria-hidden="true">#</a> **锐：</h5><blockquote><p>不错哦</p></blockquote><h5 id="宇" tabindex="-1"><a class="header-anchor" href="#宇" aria-hidden="true">#</a> *宇：</h5><blockquote><div>匹配过程应该是自上而下，因为CSSOM有继承，不知道理解的对不对  😹</div></blockquote><h6 id="讲师回复" tabindex="-1"><a class="header-anchor" href="#讲师回复" aria-hidden="true">#</a>     讲师回复：</h6><blockquote><p>    浏览器进行 CSS 选择器匹配时，是从右向左进行的，所以可以推知是自下而上</p></blockquote><h5 id="岳" tabindex="-1"><a class="header-anchor" href="#岳" aria-hidden="true">#</a> **岳：</h5><blockquote><p>应该是自下而上，如果自上而下，推测会引起重复渲染</p></blockquote><h6 id="讲师回复-1" tabindex="-1"><a class="header-anchor" href="#讲师回复-1" aria-hidden="true">#</a>     讲师回复：</h6><blockquote><p>    嗯~确实应该如此</p></blockquote><h5 id="格" tabindex="-1"><a class="header-anchor" href="#格" aria-hidden="true">#</a> **格：</h5><blockquote><p>CSSOM 树的节点具有继承特性，不是所有的样式都会继承的吧，比如 width 不能继承</p></blockquote><h5 id="_5391" tabindex="-1"><a class="header-anchor" href="#_5391" aria-hidden="true">#</a> **5391：</h5><blockquote><p>令牌化应该是 tokenize 吧，但是令牌一般指的是登录时候所说的那个 token，和编译中所说的 token 是两回事，这里应该称呼为“单词”更加恰当。</p></blockquote><h6 id="讲师回复-2" tabindex="-1"><a class="header-anchor" href="#讲师回复-2" aria-hidden="true">#</a>     讲师回复：</h6><blockquote><p>    也可以称作  “分词”。这一块没有找到比较权威的翻译说明，大家重点还是理解这个意思~</p></blockquote><h5 id="贵" tabindex="-1"><a class="header-anchor" href="#贵" aria-hidden="true">#</a> **贵：</h5><blockquote><p>自上而下，执行到有外部 css,会先读取外部 css</p></blockquote><h5 id="兵" tabindex="-1"><a class="header-anchor" href="#兵" aria-hidden="true">#</a> **兵：</h5><blockquote><p>挺好的</p></blockquote><h5 id="鸣" tabindex="-1"><a class="header-anchor" href="#鸣" aria-hidden="true">#</a> **鸣：</h5><blockquote><p>是从根节点开始匹配的，自下而上</p></blockquote><h5 id="坤" tabindex="-1"><a class="header-anchor" href="#坤" aria-hidden="true">#</a> *坤：</h5><blockquote><p>很简洁</p></blockquote><h5 id="鹏" tabindex="-1"><a class="header-anchor" href="#鹏" aria-hidden="true">#</a> **鹏：</h5><blockquote><p>最后思考题的答案是什么？</p></blockquote><h6 id="讲师回复-3" tabindex="-1"><a class="header-anchor" href="#讲师回复-3" aria-hidden="true">#</a>     讲师回复：</h6><blockquote><p>    自下而上</p></blockquote><h5 id="程" tabindex="-1"><a class="header-anchor" href="#程" aria-hidden="true">#</a> *程：</h5><blockquote><p>自下而上，只有自下上，父元素才能获得子元素的所有属性以及布局，然后才能获得对应父元素的布局，比如文中就提到 z-index。</p></blockquote><h6 id="讲师回复-4" tabindex="-1"><a class="header-anchor" href="#讲师回复-4" aria-hidden="true">#</a>     讲师回复：</h6><blockquote><p>    浏览器进行 CSS 选择器匹配时，是从右向左进行的，所以可以推知是自下而上</p></blockquote>`,88);function b(m,k){const s=o("ExternalLinkIcon");return d(),c("div",null,[p,a("p",null,[e("对于上面的代码，我们看到的是它的字符形式。而浏览器通过 "),r,e("接收到的文档内容是字节数据，下图是抓包工具截获的报文截图，报文内容为左侧高亮显示的区域（为了查看方便，该工具将字节数据以十六进制方式显示）。当浏览器得到字节数据后，通过“"),a("a",h,[e("编码嗅探算法"),i(s)]),e("”来确定字符编码，然后根据字符编码将字节流数据进行解码，生成截图右侧的字符数据，也就是我们编写的代码。")]),u])}const v=t(l,[["render",b],["__file","index-06.html.vue"]]);export{v as default};
